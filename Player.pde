// TODO #4: Finish the Player class
// - Add 2 floats storing the x & y positions
// - Add 1 float storing the velocity in the y direction
// - Add 2 integers storing the width and height of the player
// - See more below
class Player{
  
  // - Create the constructor for the class
  // - Include the variables x & y as inputs to the constructor 
  Player(){
    
  }
  
  // - Create a function that draws the player
  // - Remember the rectangle + fill functions!
  void drawPlayer(){
    
  }
  
  // Create a function that adjusts the players position based on gravity
  // (add it's velocity to it's position)
  // (add 0.5 to the velocity to represent gravity)
  void move(){
   
  }
  
}
